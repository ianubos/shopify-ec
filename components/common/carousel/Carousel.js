import styles from './Carousel.module.css'
import Slider from 'react-slick'

const Carousel = () => {
    const settings = {
        dots: true,
        Infinity: true,
        speed: 500
    }
    const images = []

    return (

    )
}

export default Carousel